{{ form_start(form, {attr: {
    id: 'navbar_search_form',
    class: 'relative grid w-full max-w-lg grid-cols-1 lg:max-w-xs',
    'data-live-url': path('op_webapp_articles_search_live'),
}}) }}
    <input type="search" name="{{ field_name(form.query) }}" id="{{ form.query.vars.id }}" value="{{ field_value(form.query) }}"
           class="col-start-1 row-start-1 block w-full rounded-md bg-gray-100 border border-gray-200 py-1.5 pr-3 pl-10 text-base text-gray-900 outline-1 -outline-offset-1 outline-gray-300 placeholder:text-gray-400 focus:outline-2 focus:-outline-offset-2 focus:outline-sky-600 sm:text-sm/6"
           placeholder="Rechercher..." autocomplete="off"/>
    <div class="pointer-events-none col-start-1 row-start-1 ml-3 size-5 self-center text-gray-400">
        <i class="fa-light fa-magnifying-glass"></i>
    </div>
    {# Suggestions instantanées Elasticsearch — rempli par IndexArticles.js #}
    <div id="navbar_search_results"
         class="absolute top-full left-0 right-0 z-50 mt-1 hidden max-h-96 overflow-y-auto rounded-md border border-gray-200 bg-white shadow-lg"
         role="listbox"></div>
{{ form_end(form) }}

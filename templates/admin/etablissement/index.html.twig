{% extends 'admin.html.twig' %}

{% block title %}Etablissement index{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}

{% block header %}
    {{ include('admin/dashboard/include/header.html.twig', {
        'title' : 'Gestions des établissements',
        'btns' : {
            'new': {
                'id' : null,
                'href' : path('op_webapp_etablissement_newetablissement'),
                'icon' : '<i class="fa-duotone fa-solid fa-square-plus"></i>',
                'text' : 'Créer une nouvelle page',
                'array':null
            },
            'return': {
                'id' : null,
                'href' : path('op_admin_dashboard_index'),
                'icon' : '<i class="fa-duotone fa-solid fa-chevrons-left"></i>',
                'text' : 'Retour',
                'array':null
            }
        }
    }) }}
{% endblock %}

{% block main %}
<div class="w-full">

    <h4 class="font-semibold text-sky-700">Liste des établissements</h4>
    <p class="text-sm">Créer, Modifier ou supprimer des établissements.</p>

    <div class="mt-3">
    {{ include('/admin/etablissement/include/_liste.html.twig') }}
    </div>

    <div class="mt-3">
        {# Ajout du template personnalisé pour la pagination en fin du block #}
        {{ knp_pagination_render(etablissements, 'include/pagination.html.twig') }}
    </div>

</div>
{% endblock %}
